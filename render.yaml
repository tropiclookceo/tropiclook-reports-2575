services:
  - type: web
    name: tropiclook-reports-2575
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    envVars:
      - key: API_TOKEN
        sync: false
